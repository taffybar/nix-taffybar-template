# Direnv configuration for this flake project.

# Optional machine-local overrides.
source_env_if_exists .envrc.local

# Reload when flake inputs change.
watch_file flake.nix
watch_file flake.lock

# Use devShells.<system>.default from flake.nix.
use flake .
